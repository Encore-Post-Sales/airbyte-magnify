# See [Connector Acceptance Tests](https://docs.airbyte.com/connector-development/testing-connectors/connector-acceptance-tests-reference)
# for more information about how to configure these tests
connector_image: airbyte/source-counsellink:dev
test_strictness_level: "high"
acceptance_tests:
  spec:
    tests:
      - spec_path: "source_counsellink/spec.yaml"
  connection:
    tests:
      - config_path: "integration_tests/sample_config.json"
        status: "succeed"
      - config_path: "integration_tests/invalid_config.json"
        status: "failed"
  discovery:
    tests:
      - config_path: "integration_tests/sample_config.json"
  basic_read:
    tests:
      - config_path: "integration_tests/sample_config.json"
        configured_catalog_path: "integration_tests/configured_catalog.json"
        empty_streams:
          - name: "client_contacts"
            bypass_reason: "This stream may be empty if no client contacts exist in the system"
